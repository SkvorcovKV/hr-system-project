# Инструкция по развертыванию

## 1. Продакшен настройки
- Измените `DEBUG = False`
- Создайте новый `SECRET_KEY`
- Настройте `ALLOWED_HOSTS`
- Используйте PostgreSQL вместо SQLite
- Соберите статические файлы: `python manage.py collectstatic`

## 2. Миграции данных
```bash
python manage.py migrate
python manage.py restore_test_data  # для тестовых данных
python manage.py createsuperuser

## 3. Запуск в продакшене
bash

# С использованием Gunicorn
gunicorn hr_system.wsgi:application

# Или с Docker
docker build -t hr-system .
docker run -p 8000:8000 hr-system


## ✅ **Шаг 5: Финальная проверка**

### **Чеклист готовности:**
- [ ] Табель открывается для всех пользователей
- [ ] Админ видит всех сотрудников
- [ ] Сотрудники видят только свои записи
- [ ] Навигация между месяцами работает
- [ ] Цвета типов отсутствий отображаются
- [ ] Дашборд показывает статистику
- [ ] Выход из системы работает
- [ ] Админ-панель доступна

## 🎯 **Что дальше? (опционально)**

Если хотите продолжить развитие:

1. **Годовой обзор табеля** - сводка за весь год
2. **График отпусков** - визуализация пересечений
3. **Заявки на отпуск** - workflow подачи/подтверждения
4. **Отчеты** - экспорт в Excel/PDF
5. **Уведомления** - email напоминания
6. **REST API** - для мобильного приложения

## 📞 **Поддержка**

Проект готов к использованию! Если возникнут вопросы:
- Проверьте данные в админке: http://127.0.0.1:8000/admin/
- Посмотрите вывод в терминале Django (debug сообщения)
- Проверьте структуру базы данных
