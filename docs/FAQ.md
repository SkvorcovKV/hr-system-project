# частые вопросы (например, "Как добавить тип отсутствия?")

# Частые вопросы (FAQ)

---

### ❓ Как назначить руководителя сотруднику?

1. Зайдите в **админку Django**: [http://127.0.0.1:8000/admin](http://127.0.0.1:8000/admin)
2. Перейдите в раздел **Employees → Employee**
3. Найдите нужного сотрудника и нажмите на него
4. В поле **Manager** выберите руководителя из списка
5. Нажмите **Сохранить**

> 💡 Руководитель тоже должен быть в списке `Employee`.

---

### ❓ Как добавить новый тип отсутствия?

1. Перейдите в **админку**
2. Откройте **Attendance → Absence Types**
3. Нажмите **Добавить Absence Type**
4. Заполните:
   - **Code** — например, `20`
   - **Name** — например, «Учебный отпуск»
   - **Color** — выберите цвет в формате `#RRGGBB`, например `#6f42c1`
5. Сохраните

> ✅ Новый тип сразу появится в табеле.

---

### ❓ Почему сотрудник не видит подчинённых?

- Убедитесь, что у **руководителя** установлен флаг `is_staff=False`, но он **должен быть в `Employee`**
- Проверьте, что у подчинённых указан **этот сотрудник как `manager`**
- Убедитесь, что в `views.py` используется проверка `user.profile.is_manager_of(employee)`

---

### ❓ Как сбросить базу данных?

> ⚠️ Внимание: все данные будут удалены!

```bash
# Удалить базу
Remove-Item db.sqlite3

# Удалить миграции (опционально)
Remove-Item attendance/migrations/0*.py
Remove-Item employees/migrations/0*.py

# Создать заново
python manage.py makemigrations
python manage.py migrate
python manage.py createsuperuser


❓ Как восстановить данные из backup.json?
bash
python manage.py loaddata backup.json
✅ Подходит для переноса тестовых данных или восстановления.

❓ Как изменить цвет статуса в табеле?
Перейдите в Absence Types в админке
Выберите нужный тип (например, «Больничный»)
Измените поле Color на нужный HEX-код
Сохраните
🎨 Цвет автоматически обновится в интерфейсе.