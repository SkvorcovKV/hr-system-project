# Статус проекта HR Система

## 🏗️ **Текущая версия:** v0.2
**Дата:** 04.12.2025
**Состояние:** Рабочий прототип

## ✅ **Реализованные функции:**

### 1. **Базовый функционал:**
- [x] Django проект настроен и работает
- [x] База данных SQLite настроена
- [x] Виртуальное окружение настроено

### 2. **Аутентификация и авторизация:**
- [x] Система входа/выхода
- [x] Разделение прав доступа
- [x] Панель администратора Django

### 3. **Модели данных:**
- [x] Модель `EmployeeProfile` (профиль сотрудника)
- [x] Модель `Department` (отдел)
- [x] Модель `AbsenceType` (тип отсутствия)
- [x] Модель `AttendanceRecord` (запись в табеле)

### 4. **Пользовательский интерфейс:**
- [x] Дашборд с информацией о пользователе
- [x] Навигационное меню
- [x] Адаптивный дизайн (Bootstrap 5)
- [x] Страница входа

### 5. **Тестовые данные:**
- [x] Созданы типы отсутствий (6 видов)
- [x] Создан отдел "техническая поддержка"
- [x] Созданы 3 тестовых сотрудника
- [x] Добавлены тестовые записи в табель

## 📊 **Созданные данные:**

### **Типы отсутствий:**
1. Отпуск основной (09) - #009900
2. Больничный (19) - #660000  
3. Удаленная работа (38) - #006666
4. Командировка (06) - #003300
5. Отпуск за свой счет (16) - #99004C
6. Работал в дневное время (01) - #00FF00

### **Отдел:**
- Название: техническая поддержка

### **Сотрудники:**
1. Синцов Андрей Владимирович
2. Терентьев Дмитрий Васильевич  
3. Юнисов Рамиль Наилевич

### **Записи в табеле:**
- Все 3 сотрудника работали в дневное время 04.12.2025 и 05.12.2025

## 🔧 **Технические характеристики:**

### **Стек технологий:**
- **Backend:** Django 5.2, Python 3.13
- **Frontend:** HTML, Bootstrap 5, JavaScript
- **Database:** SQLite (разработка)
- **Version Control:** Git, GitHub

### **Структура проекта:**
hr-system-project/
├── attendance/ # Учет рабочего времени
├── employees/ # Сотрудники и отделы
├── hr_system/ # Настройки проекта
├── templates/ # HTML шаблоны
│ ├── auth/ # Страницы входа/выхода
│ └── attendance/ # Шаблоны учета
├── media/ # Загружаемые файлы
├── static/ # Статические файлы
├── .gitignore
├── manage.py
├── requirements.txt
└── README.md

## 🚀 **Инструкция по восстановлению проекта:**

### **1. Клонирование и настройка:**
```bash
git clone https://github.com/SkvorcovKV/hr-system-project.git
cd hr-system-project
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt

### **2. Настройка базы данных:**
python manage.py migrate
python manage.py createsuperuser

### **3. Восстановление тестовых данных:
# В Python shell выполните:
from attendance.models import AbsenceType
from employees.models import Department, EmployeeProfile
from django.contrib.auth.models import User
from datetime import date

# 1. Типы отсутствий
types = [
    {'name': 'Отпуск основной', 'code': '09', 'color': '#009900'},
    {'name': 'Больничный', 'code': '19', 'color': '#660000'},
    {'name': 'Удаленная работа', 'code': '38', 'color': '#006666'},
    {'name': 'Командировка', 'code': '06', 'color': '#003300'},
    {'name': 'Отпуск за свой счет', 'code': '16', 'color': '#99004C'},
    {'name': 'Работал в дневное время', 'code': '01', 'color': '#00FF00'},
]

for i, data in enumerate(types):
    AbsenceType.objects.get_or_create(
        code=data['code'],
        defaults={**data, 'is_paid': True, 'requires_approval': True, 'order': i+1}
    )

# 2. Отдел
dept, _ = Department.objects.get_or_create(
    name='техническая поддержка',
    defaults={'description': 'Отдел технической поддержки'}
)

# 3. Сотрудники
employees_data = [
    'Синцов_Андрей_Владимирович',
    'Терентьев_Дмитрий_Васильевич', 
    'Юнисов_Рамиль_Наилевич'
]

for emp_name in employees_data:
    username = emp_name.lower().replace('_', '')
    user, _ = User.objects.get_or_create(
        username=username,
        defaults={'first_name': emp_name.split('_')[1], 'last_name': emp_name.split('_')[0]}
    )
    
    EmployeeProfile.objects.get_or_create(
        user=user,
        defaults={
            'department': dept,
            'position': 'Специалист техподдержки',
            'hire_date': date.today()
        }
    )
4. Запуск:
bash
python manage.py runserver
📝 Следующие шаги разработки:
Высокий приоритет:
Реализация месячного табеля учета

Форма добавления/редактирования записей

Разграничение прав (сотрудник/руководитель)

Средний приоритет:
Годовой обзор табеля

График отпусков

Заявки на отпуск

Низкий приоритет:
Отчеты и экспорт

Уведомления

Календарная интеграция

🐞 Известные проблемы:
Форма выхода требует POST запрос (исправлено через скрытую форму)

Табель учета находится в разработке

👥 Контакты:
Разработчик: Konstantin Skvortcov

GitHub: https://github.com/SkvorcovKV

Проект: https://github.com/SkvorcovKV/hr-system-project


## 📦 **Шаг 2: Создана команда для восстановления данных**

Файл `attendance/management/commands/restore_test_data.py`:

Использование:

bash
python manage.py restore_test_data